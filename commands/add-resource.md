---
name: add-resource
description: Add a resource or resource template to an MCP server
---

# /add-resource

Add read-only data to an MCP server as a resource.

## Workflow
1. Understand — what data? static or template? format?
2. Design URI scheme
3. Generate handler file
4. Register with `server.registerResource()`
5. Test and verify
