---
name: add-prompt
description: Add a prompt template to an MCP server
---

# /add-prompt

Add a reusable prompt template.

## Workflow
1. Understand — what workflow? what arguments?
2. Design args schema
3. Generate prompt handler
4. Register with `server.registerPrompt()`
5. Test and verify
